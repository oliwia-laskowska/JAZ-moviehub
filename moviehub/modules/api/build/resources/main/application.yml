
spring:
  application:
    name: moviehub
  thymeleaf:
    cache: false

  tmdb:
      base-url: https://api.themoviedb.org/3
      api-key: ${TMDB_API_KEY}
      language: en-US


management:
  endpoints:
    web:
      exposure:
        include: health,info
